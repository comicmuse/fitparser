# Agent Instructions

- The Python executable for this project is `python3`.
- Always activate and run commands within the virtual environment located at `.venv`.
- To activate the virtual environment, use:
  
  ```bash
  source .venv/bin/activate
  ```
- All Python scripts and servers should be run using `python3` after activating the virtual environment.
